# TAAC Demo 数据集统计分析（`dataset/data_sample_1000/demo_1000.parquet`）

> 目的：基于样例数据做可复现的统计画像，并结合当前 baseline 行为给出可落地建模启发。  
> 分析环境：`conda activate taac`（Python 3.11.15 / pandas 2.3.3 / pyarrow 23.0.1 / numpy 2.2.5）  
> 统计时间：2026-04-26

---

## 1. 数据整体概况

| 指标 | 数值 |
|---|---:|
| 样本数 | 1000 |
| 列数 | 120 |
| 正样本率（`label_type==2`） | 12.4% |
| 负/正样本 | 876 / 124 |
| 用户数（`user_id`） | 1000 |
| 物料数（`item_id`） | 837 |
| 每用户平均样本数 | 1.00 |
| 每物料平均样本数 | 1.19 |

**观察**
- 样例集是强稀疏、强不平衡的小样本切片（正样本仅 `12.4%`）。
- 用户在样例中几乎“一人一条”，用户级泛化能力难从该样本稳定估计。

---

## 2. 字段分组计数核验

| 分组 | 列数 |
|---|---:|
| `user_int_feats_*` | 46 |
| `user_dense_feats_*` | 10 |
| `item_int_feats_*` | 14 |
| `domain_a_seq_*` | 9 |
| `domain_b_seq_*` | 14 |
| `domain_c_seq_*` | 12 |
| `domain_d_seq_*` | 10 |

**结论**：与官方样例说明一致（120 列结构完整）。

---

## 3. 时间分布与标签时延

| 指标 | 数值 |
|---|---:|
| `timestamp` 跨度 | 781 秒 |
| `label_time` 跨度 | 883 秒 |
| `label_time - timestamp` 最小/中位/P95/最大 | 2 / 191.5 / 549.1 / 832 秒 |

**观察**
- 样本处于非常短的时间窗口内（约 15 分钟）。
- 标签事件普遍晚于请求时刻，存在明显时延分布，可作为监督信号的一部分。

---

## 4. 缺失值画像

### 4.1 分组级缺失率

| 分组 | 平均缺失率 | 中位缺失率 | 最大缺失率 |
|---|---:|---:|---:|
| `user_int` | 28.27% | 15.50% | 91.00% |
| `user_dense` | 9.89% | 7.00% | 45.00% |
| `item_int` | 21.11% | 0.20% | 83.20% |
| `domain_a_seq` | 0.50% | 0.50% | 0.50% |
| `domain_b_seq` | 1.20% | 1.20% | 1.20% |
| `domain_c_seq` | 0.20% | 0.20% | 0.20% |
| `domain_d_seq` | 8.00% | 8.00% | 8.00% |

### 4.2 Top 缺失列（前 10）

`user_int_feats_101(91.0%)`、`102(87.7%)`、`103(86.2%)`、`109(85.4%)`、`100(84.5%)`、`item_int_feats_83/84/85(83.2%)`、`user_int_feats_99(81.2%)`、`user_int_feats_86(69.2%)`。

**观察**
- `user_int` 的长尾高缺失最明显。
- `item_int_feats_83/84/85` 三列共缺失同频，疑似“同一子场景条件下同时出现”。

---

## 5. List 特征长度特性

### 5.1 关键 `user_int` list 列

| 列 | 均值长度 | P95 | Max | 空值比例 |
|---|---:|---:|---:|---:|
| `user_int_feats_15` | 3.23 | 8 | 13 | 13.9% |
| `user_int_feats_60` | 0.62 | 2 | 2 | 59.2% |
| `user_int_feats_62` | 2.11 | 4 | 5 | 7.0% |
| `user_int_feats_66` | 7.14 | 21.05 | 66 | 8.6% |
| `user_int_feats_89` | 9.45 | 10 | 10 | 5.5% |
| `user_int_feats_91` | 5.50 | 10 | 10 | 45.0% |

### 5.2 `user_dense` 长度特性

| 列 | 均值长度 | P95 | Max | 空值比例 |
|---|---:|---:|---:|---:|
| `user_dense_feats_61` | 255.49 | 256 | 256 | 0.2% |
| `user_dense_feats_87` | 315.20 | 320 | 320 | 1.5% |
| `user_dense_feats_62` | 2.11 | 4 | 5 | 7.0% |
| `user_dense_feats_66` | 7.14 | 21.05 | 66 | 8.6% |
| `user_dense_feats_91` | 5.50 | 10 | 10 | 45.0% |

**观察**
- `61/87` 是高维稠密向量；`62-66,89-91` 明显是变长“槽位式”向量。
- `91` 在 int 与 dense 两路都高缺失（45%），是典型冷启动/稀疏信号。

---

## 6. `user_int` 与 `user_dense` 对齐关系（核心）

共享 fid：`62,63,64,65,66,89,90,91`。

| fid | 长度完全一致比例 | 同时非空比例 | 同时空比例 | 均值长度 |
|---:|---:|---:|---:|---:|
| 62 | 100% | 93.0% | 7.0% | 2.11 |
| 63 | 100% | 93.0% | 7.0% | 2.56 |
| 64 | 100% | 93.0% | 7.0% | 3.82 |
| 65 | 100% | 92.0% | 8.0% | 5.71 |
| 66 | 100% | 91.4% | 8.6% | 7.14 |
| 89 | 100% | 94.5% | 5.5% | 9.45 |
| 90 | 100% | 90.9% | 9.1% | 9.09 |
| 91 | 100% | 55.0% | 45.0% | 5.50 |

**结论**
- 强证据支持“同一 fid 的 int/dense 是对齐信号（key-value 或 id-weight）”。
- 这与 baseline `ns_groups.json` 的注释一致：共享 fid 的 int 与 float 共同描述同一语义。

---

## 7. 四域序列统计

### 7.1 长度分布

| 域 | 均值 | P50 | P95 | P99 | Max | 空值比例 |
|---|---:|---:|---:|---:|---:|---:|
| `domain_a` | 701.09 | 577.5 | 1673.15 | 1793.02 | 1888 | 0.5% |
| `domain_b` | 570.76 | 405.0 | 1563.15 | 1803.00 | 1952 | 1.2% |
| `domain_c` | 449.43 | 322.0 | 1214.25 | 2526.48 | 3894 | 0.2% |
| `domain_d` | 1099.86 | 1035.5 | 2451.10 | 2865.13 | 3951 | 8.0% |

### 7.2 0 值占比（域内列均值）

| 域 | 平均 0 比例 | 最高列 0 比例 | 最低列 0 比例 |
|---|---:|---:|---:|
| `domain_a` | 37.88% | 95.85% (`domain_a_seq_45`) | 0% (`domain_a_seq_39/41`) |
| `domain_b` | 40.54% | 89.26% (`domain_b_seq_74`) | 0% (`domain_b_seq_67`) |
| `domain_c` | 10.97% | 69.72% (`domain_c_seq_31`) | 0% (`domain_c_seq_27/28/32`) |
| `domain_d` | 28.48% | 93.31% (`domain_d_seq_22`) | 0% (`domain_d_seq_17/26`) |

### 7.3 时间戳序列有序性

对 `domain_a_seq_39`、`domain_b_seq_67`、`domain_c_seq_27`、`domain_d_seq_26` 检查“是否单调非增”：
- 四个域通过率均为 **100%**（检查样本分别 993/985/997/916 行）。

**结论**
- 序列时序非常规整，可直接利用“最近行为更重要”的建模假设。
- 不同域长度和稀疏度差异极大，统一截断策略会产生域偏置。

---

## 8. 跨域相关性与新鲜度（Recency）

### 8.1 域长度相关系数（Pearson）

|  | a | b | c | d |
|---|---:|---:|---:|---:|
| a | 1.000 | 0.746 | 0.200 | 0.095 |
| b | 0.746 | 1.000 | 0.274 | 0.103 |
| c | 0.200 | 0.274 | 1.000 | 0.023 |
| d | 0.095 | 0.103 | 0.023 | 1.000 |

### 8.2 请求时刻与“最近一次行为”的间隔（秒）

| 域 | 有效样本比例 | P50 gap | P95 gap |
|---|---:|---:|---:|
| a | 99.5% | 20,344 | 531,660 |
| b | 98.8% | 30,570.5 | 5,588,927.5 |
| c | 99.8% | 130,690.5 | 1,298,796.5 |
| d | 92.0% | 450.5 | 119,570.6 |

**观察**
- `domain_d` 行为最“新鲜”（中位 gap 仅 7.5 分钟），其余域多是历史长尾行为。

---

## 9. 与标签的统计关联（样例级）

### 9.1 按序列长度四分位的正样本率

| 域 | Q1 | Q2 | Q3 | Q4 |
|---|---:|---:|---:|---:|
| `domain_a` | 0.112 | 0.132 | 0.112 | 0.140 |
| `domain_b` | 0.140 | 0.092 | 0.140 | 0.124 |
| `domain_c` | 0.116 | 0.100 | 0.128 | 0.152 |
| `domain_d` | 0.100 | 0.148 | 0.108 | 0.140 |

**观察**
- 长度与标签有关，但关系非单调、域间异质性明显（小样本噪声也较大）。

### 9.2 缺失即信号（部分高缺失列）

`delta = pos_rate(出现) - pos_rate(缺失)`：
- `user_int_feats_104`: `+0.0391`
- `user_int_feats_100`: `+0.0365`
- `user_int_feats_105`: `+0.0343`
- `item_int_feats_83/84/85`: `-0.0346`（出现时反而更低）
- `user_int_feats_96`: `-0.0363`

**结论**
- 缺失机制显著非随机，建议显式建模 missing indicator。

### 9.3 选定离散列的标签率差异

- `item_int_feats_13` 各取值差异很大（例如 `4.0` 的正样本率远高于 `2.0/6.0`）。
- `item_int_feats_81` 中 `2.0` 的正样本率明显低于 `0.0/1.0`。

> 注：非常低频取值（如 NaN 仅 2 条）不具稳健性，需谨慎解释。

---

## 10. Dense 向量范数特性

| 列 | 有效比例 | 均值范数 | P50 | P95 |
|---|---:|---:|---:|---:|
| `user_dense_feats_61` | 99.8% | 0.9977 | 0.9980 | 1.0000 |
| `user_dense_feats_87` | 98.5% | 1.8884 | 2.0000 | 2.8284 |
| `user_dense_feats_89` | 94.5% | 1.0000 | 1.0000 | 1.0000 |
| `user_dense_feats_90` | 90.9% | 1.0000 | 1.0000 | 1.0000 |
| `user_dense_feats_91` | 55.0% | 1.0000 | 1.0000 | 1.0000 |

**观察**
- 多个 dense 向量范数几乎固定，表明“方向信息”可能比“模长信息”更关键。

---

## 11. 对 baseline 的直接启发（按优先级）

### P1（建议先做）
- 共享 fid（`62-66,89-91`）做 **pair fusion**（int id 与 dense value 对位融合），替代“int 与 dense 完全分路后晚融合”。
- 对高缺失列加 **missing indicator**（尤其 `83-109` 段及共享 fid 的稀疏位）。
- 序列截断改为 **域自适应**（`d` 更长、`c` 长尾更厚），避免统一长度策略。

### P2（可并行）
- 对高零占比列引入门控或列权重，减小近常量列噪声。
- 对标签差异明显的低基数离散列（如 `item_int_feats_13/81`）做更细粒度 token/group。

---

## 12. 可信度与边界

- 本文所有数值均来自真实脚本统计（非人工估算）。
- 但样例仅 `1000` 行，方差较大；适合做**方向判断和结构设计**，不适合做最终效果结论。
- 建议后续在全量数据上复核：缺失机制、共享 fid 对齐规律、域长度分布、标签关联方向。

---

## 13. 复现方式（同环境）

```bash
source /home/lgh/anaconda3/etc/profile.d/conda.sh
conda activate taac
python - <<'PY'
import pandas as pd
df = pd.read_parquet('/home/grf/TAAC-main/dataset/data_sample_1000/demo_1000.parquet')
print(df.shape, df['label_type'].value_counts().to_dict())
PY
```

> 本次完整统计原始汇总已输出到：`/tmp/taac_demo_stats.json`。
